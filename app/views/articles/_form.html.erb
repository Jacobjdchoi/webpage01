<%= nested_form_for(@article) do |f| %>
    <% if @article.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@article.errors.count, "에러들") %> 때문에 저장하지 못했습니다:</h2>
          <ul>
          <% @article.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
    <% end %>
    <div class="translationForm">
        <div class="koreanForm">
            <h3>한글</h3>
            <div>
                <%= f.label :title_ko, "한글 제목" %>
                <%= f.text_field :title_ko %>
            </div>
            <div>
                <label for="body">한글 내용</label>
                <%= f.text_area :body_ko, class: "articleBody" %>
            </div>
        </div>
        <div class="enlgishForm">
            <h3>영문</h3>
            <div>
                <%= f.label :title_en, "영문 제목" %>
                <%= f.text_field :title_en %>
            </div>
            <div>
                <label for="body">영문 내용</label>
                <%= f.text_area :body_en, class: "articleBody" %>
            </div>
        </div>
    </div>
    <div class="photoForm">
        <h3>부수 사진</h3>
        <i>사진추가시 등록이후 아미지 사이즈 조정 페이지로 넘어감.</i>
        <%= f.fields_for :photos do |pf| %>
            <%= pf.file_field :pics, class: "photoInfo" %>
            <%= pf.link_to_remove "삭제하기", class: "photoButton", type: "remove" %>
        <% end %>
        <%= f.link_to_add "추가하기", :photos, class: "photoButton", type: "add" %>
    </div>
    <div>
        <%= f.submit "등록하기" %>
    </div>
<% end %>