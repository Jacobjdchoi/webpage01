<h2 class="page-title"><%= t(".title") %></h2>
<div class="history-contents-container">
  <table>
    <tr>
      <td><strong><%= l @milestone1, format: :short %></strong></td>
      <td><%= t('.milestone1') %></td>
    </tr>
    <tr>
      <td><strong><%= l @milestone2, format: :short %></strong></td>
      <td><%= t('.milestone2') %></td>
    </tr>
    <tr>
      <td><strong><%= l @milestone3, format: :short %></strong></td>
      <td><%= t('.milestone3') %></td>
    </tr>
    <tr>
      <td><strong><%= l @milestone4, format: :short %></strong></td>
      <td><%= t('.milestone4') %></td>
    </tr>
    <tr>
      <td><strong><%= l @milestone5, format: :short %></strong></td>
      <td><%= t('.milestone5') %></td>
    </tr>
    <tr>
      <td><strong><%= l @milestone6, format: :short %></strong></td>
      <td><%= t('.milestone6') %></td>
    </tr>
    <tr>
      <td><strong><%= l @milestone7, format: :short %></strong></td>
      <td><%= t('.milestone7') %></td>
    </tr>
  </table>
</div>